#include<stdio.h>
int main()
{
    int n,m,rev=0,temp;
    printf("enter a number : ");
    scanf("%d",&n);
    while(n>0)
    {
        m=n%10;
        rev=(rev*10)+m;
        n=n/10;
    }
    printf("reverse: %d",rev);
}
